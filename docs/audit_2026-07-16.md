# Full-system audit — 2026-07-16

Scope: the whole repo at `2fc3229`, audited against the project's stated goals.
Method: 4 parallel adversarial code audits + live driving of the real MCP surface
(`resolve_tool`, `agent_status`) + inspection of the real artifacts on disk
(`env_reports/`, `_env_cache.json`). Every claim below was verified by execution
or by reading code — not from docstrings, not from CLAUDE.md.

Baseline: `pytest tests/` = **1251 passed, 1 skipped** (110s). Read that number
with §2 in mind.

---

## 0. The goals, as I understand them

1. **Any tool, however gnarly → a reproducible compute env.** Not "install a tool";
   install it so that the artifact is portable to HPC and byte-identical to what
   was validated.
2. **Scripts + user guides** so a bioinformatician can run that tool on *their* data.
3. **A solved component**: call once per tool/version, get a trustworthy artifact,
   *never look at it again*.
4. Trust must be **machine-verified, not agent-asserted** — because (3) means nobody
   is going to re-check. This is why the honesty contract exists.

(3)+(4) are the differentiator. The product is not the env; it's *not having to
wonder about the env*.

## 1. Verdict

| Goal | State |
|---|---|
| 1 — reproducible envs | **Strong.** Container-native build, install==ship, content-addressed cache, 10 real install tiers, cross-arch. Genuinely good, genuinely novel. |
| 2 — scripts & user guides | **Not built.** The only real user guide in the repo was hand-written by an agent in chat, is gitignored, and no code path can produce it. |
| 3 — solved component | **Blocked by 4.** |
| 4 — machine-verified trust | **Structurally sound, effectively perforated.** See §2 and §3. |

The engineering quality is high. The failures are not sloppiness — they are all
the *same* failure, repeated (§2), plus one missing concept (§3).

---

## 2. THE SYSTEMIC FINDING: gates that are present in the code and absent in effect

Seven independent instances. In every one, a check exists, reads as enforced, is
described as enforced in CLAUDE.md — and does not fire. The pattern is always the
same: a guard was made non-fatal to avoid friction, in a way that silently
disables it instead of failing loudly.

| # | Gate | Why it doesn't fire | Verified by |
|---|---|---|---|
| **1** | **The reliability gate** (`author_image`/`authors_recipe` — the flagship feature of the last 5 commits) | `resolver.py:728` calls `probe_authors_sources(..., timeout=timeout)`; `assess_tool_sources` has **no `timeout` param** → `TypeError` every call → swallowed by bare `except Exception: pass` at `resolver.py:737`. **Never fires, for any tool, ever.** | Live: `resolve('talos', github_repo=...)` → `chosen: synthesis`, no author tiers in `probed`. Dropping the kwarg in-memory → `chosen: authors_recipe`. |
| **2** | `author_image` availability | `authors_sources.py:210` hits `api.github.com/orgs/{owner}/packages` → **401 unauthenticated**; no token plumbing exists (`GITHUB_TOKEN` = 0 hits repo-wide). Also `/orgs/` only, so user-owned repos excluded. Dead even after fixing #1. | curl → 401 |
| **3** | Cross-namespace collision guard | `resolver.py:751`: `if github_repo:` — only runs when the caller already passed the repo, i.e. when they already know the answer. Disabled in the common case. | `resolve('cellranger')` → `collisions: []` |
| **4** | **I4 — the usage self-test seal gate** | `workflow_tools.py:213`: `if draft.get("usage") and draft.get("conda_env"):`. `self_test_usage` requires a **host conda env** (`spec_writer.py:95-97`). The architecture moved container-native; the primary path has no host env → gate skipped silently, seal proceeds, unverified how-to ships. | 3 of 4 sealed workflows on disk have `usage_verified=False`. |
| **5** | **VALIDATED_IN_IMAGE on the adopt path** | `check_adopt` deliberately omits it (`env_honesty.py:332-345`). Adopt is the *default* path for pure-conda tools. | `_env_cache.json`: `samtools=1.21` → `verifications: []`. Zero. |
| **6** | **I13 license firewall on `freeze_from_image`** | `freeze_record` emits key **`gated`**; `_check_license` reads **`license_gated`** (`env_honesty.py:251`). Key never set on that path → gated artifact with `licenses: []` registers clean. `freeze()` sets it correctly; only the newest path is broken. | Executed the real record shape → `violations: NONE`. |
| **7** | Evidence **depth** | `evidence_depth`/`is_shallow_evidence` are computed and rendered as a tooltip. **Never gated.** All 3 real envs in the cache are validated by shallow evidence only. | `grep` — no non-render caller. |
| **8** | **I7 — resource honesty** | `spec_writer.py:609` uses `and` where its own message says *"a process that actually ran has a nonzero peak RSS **and** wall"* — it only rejects when **both** are zero. `cluster_jobs.py:398` falls back to `rows[-1]` on missing MaxRSS, yielding `peak_rss_mb=0.0` with no `sacct_error`. On any cluster without cgroup MaxRSS accounting, **I7 seals a fabricated zero RSS as honest cost data.** | Executed: `peak_rss_mb=0.0, wall=12.3` → `I7 violations: NONE`. |

### Why 1251 green tests didn't catch any of this

`tests/test_authors_sources.py:98-99`:
```python
monkeypatch.setattr(R, "probe_authors_sources",
                    lambda tool, owner="", repo="", timeout=12: A.assess_tool_sources(...))
```
The test double **invents the `timeout` kwarg the real function lacks**. The mock
encodes the signature the caller *wishes for*; production throws. 9 tests pass
against a dead path.

This is systemic, not a one-off: **395 `monkeypatch.setattr` sites, 137 of them
lambda doubles**, none signature-enforced. The suite substantially tests the
system's mocks rather than the system.

Sharpest detail: `assess_tool_sources` already provides a proper injection seam
(`sources=`, `get_text=`, `get_json=` are parameters, designed for exactly this).
The tests bypassed the seam the code offered in favor of replacing the reference —
which is the only way this class of drift can occur.

---

## 3. THE CONCEPTUAL HOLE: the contract verifies integrity, never identity

Every layer verifies **"the thing we installed works."** No layer verifies
**"the thing we installed is the thing you asked for."**

Live, right now, on the real MCP surface:

```
resolve_tool("cellranger") → install_r_package(env, "cellranger", source="cran")
                             # rsheets/cellranger — an R package for parsing
                             # SPREADSHEET CELL RANGES. Not 10x Genomics Cell Ranger.
                             # (CLAUDE.md itself lists cellranger as install_release_binary.)

resolve_tool("talos")      → install_pip_package(env, "talos", version="1.4")
                             # autonomio's Keras hyperparameter tuner. Not the
                             # rare-disease pipeline this project was built around.
```
Both return `cross_namespace_collisions: []`.

Now follow either through the system. `library(cellranger)` loads fine. So it
**BUILDs**. Its evidence passes in the image, so it's **VALIDATED_IN_IMAGE**. No
accel/license claim, so **POLICY_CLEAN**. It gets a content digest, an in-toto/SLSA
attestation, an ENV.html reading *"✓ Validated in shipped image"*, a recipe.yaml,
and an Apptainer .sif on the cluster.

**A fully green, digest-pinned, attested artifact containing the wrong software.**

`grep identity` across `env_honesty.py` + `spec_writer.py` → nothing. The entire
trust apparatus rests on an unverified `tool_name → registry_entry` assumption, and
bioinformatics tool names collide constantly. This is the most user-facing defect in
the system: it is the one that hands you a bad artifact while telling you it's good,
which is precisely the outcome the project exists to prevent.

The fix is not more verification machinery — it's one new question asked early:
*does this registry entry's description/repo plausibly correspond to the tool the
user asked for?* Cheap signals exist (summary text, homepage repo, whether the repo
is bio-adjacent). Ambiguity should **refuse and ask**, not silently pick.

---

## 4. Goal 2 is not built

The single best user-facing artifact in the repo is
`env_reports/talos_authors.USER_GUIDE.md` (20 KB, genuinely excellent — explains
what Talos does, PED column semantics, that `.mt` is a directory, that `$HOME`
isn't writable on the cluster, that an empty result is a valid answer).

- `grep -rn "USER_GUIDE" --include="*.py"` → **zero hits**. No code produces it.
- `.gitignore:47` → **untracked**.
- It was hand-written in a chat session.

Meanwhile the *generated* Layer-2 deliverable (`RUN.html`) :
- renders **one** command (`usage.command_template: str` — the model is singular, so
  a Phase A→B→C pipeline is **not expressible even in principle**);
- documents 1 of Talos's 16 real steps and titles it "the workflow";
- renders `Command not self-tested` and *"Self-tested against 1 declared input
  shape(s)"* in the same panel (`run_dashboard_html.py:235-236`, unconditional);
- footers *"no field on this page was authored by the agent"* — false; it renders
  `description`, `usage.*`, all of which CLAUDE.md lists in `patch_pipeline`'s
  agent-authored allowlist.

**All 4 sealed workflows on disk have exactly 1 pipeline step.** The system has
never sealed a multi-step pipeline. For a tool whose stated purpose includes
"pipeline generation", that is the biggest unproven claim in the repo.

And the user's own stated preference — *"vanilla commands first; nextflow is a
bonus; the user guide = vanilla how-to first"* — was inverted: the markdown guide
was retired and a test now **enforces** the retirement
(`test_end_to_end_seal.py:225`), while Nextflow is the only orchestration renderer
that exists. There is no `run.sh`. The "bonus" shipped; the "first" didn't.

### The root cause is a category error
A **verified-run dashboard** (an audit trail: what ran, where, at what digest) was
asked to double as a **how-to** (a teaching document: what this tool does, what a
pedigree column means, what an empty result means). They have different sources of
truth. A dashboard can only render what the invariants captured. A how-to needs
judgment no invariant can capture.

"No field authored by the agent" is the **right rule for an attestation and the
wrong rule for a guide.** The system enforced purity everywhere, so the guide had
to be written outside the system — and it was. The fix is to let the guide be
agent-authored and *labelled as such*, sitting beside the verified evidence rather
than pretending to be derived from it.

---

## 5. Over-engineering / dead weight

- `env_vendor.py` — 77 lines, 64 of them a docstring for an unimplemented feature.
  **Zero live importers.** Exactly what `feedback_architecture_discipline` forbids.
- `env_freeze.build_env_from_tools` — advertised in CLAUDE.md's freeze row as a
  live sibling; **no MCP tool exposes it**. Documented but unreachable.
- **69 MCP tools exposed**; CLAUDE.md presents ~25 as "the only tools the agent
  needs". The tool list *is* the agent's prompt; 69 is a routing liability.
- `resolve_tool`'s live docstring — the thing the agent actually routes on — still
  describes the **pre-gate** ranking (`conda > pip/cran > binary > source > manual`).
  It never mentions author tiers, spack, or synthesis.
- **4 implementations** of "does this image exist / what's its digest"
  (`container_build`, `docker_builder`, `freeze_from_image`, a closure in
  `freeze_tools`).
- **2 EnvCache record builders with divergent `mode` vocabularies**
  (`"adopt"|"build"` vs `"container-native"`); 5 consumers branch on `mode` and
  none handle the latter. Latent landmine.
- `assess_tool_sources` does **17 serial GitHub GETs per resolve** (12s timeout
  each) on a *query-only* tool; ~10 are never read by any routing decision.
- `spack` tier: probes the v1.0 repo layout, builds from `spack@v0.22.1`'s
  **different** layout; no `-`→`_` normalization, so every `py-*`/`r-*` package
  probes unavailable. Never real-build tested. Self-described as "ADVISORY" while
  being rankable.
- 7 artifacts per env/workflow. The count isn't the problem — **7 artifacts and not
  one tells a user how to run a 16-step pipeline** is the problem.

## 6. Other real bugs found

- `env_freeze.py:644` — `_R_TIERS = {"cran", "bioconductor"}` omits **`r_github`** →
  builds an image that runs `Rscript` with **no R installed**. Live today.
- `route()` has no `author_image`/`authors_recipe` branch → the moment #1 is fixed,
  `build_env_from_tools` starts **refusing top-ranked tools** with a stale message.
- `_image_digest` uses `{{index .Id}}` (local config ID), not `RepoDigests` → the
  recorded digest for an adopted image is **not a pullable manifest digest**;
  "adopt by digest" is nominal there.
- `authors_sources.py:118-120` — operator-precedence bug: `A or (B and C)` flags
  LICENSE files and shell scripts as binaries while **not** flagging genuine source
  tarballs — the exact inverse of the docstring.
- `env_recipe_render.py:320` tells the user to run `verify_env_recipe`, which for
  `authors-dockerfile` is hard-coded **not to verify**.
- `recipe.md` says the Dockerfile is "included alongside this recipe" — no such file
  is written.
- CLAUDE.md:62 claims recipes are written "ALWAYS, for EVERY install path" — false
  of the corpus on disk (nothing backfills; 2 of 3 envs lack `recipe.md`).

---

## 6b. The HPC bridge — better than expected; trim the unreachable half

**Genuinely proven against the real cluster** (artifacts, not claims): 4 sealed
cluster-locus workflows with real SLURM job IDs + node names, all
`validated_in_shipped_image: true` with `cluster_sif_sha256` round-tripped; 16 real
`acquire_*` SLURM submissions; **92 real Globus transfers** including a 1.0 GB tar
and an 888 MB .sif. All four zones carry real traffic. `run_step_on_cluster` is the
best-proven component in the repo. **The four-zone auth model is earned** — ~80 LOC,
real traffic in each, genuinely distinct trust semantics. This is *not* empire; it's
the honesty contract's cluster arm.

**Unreachable / never-run (~600–800 LOC of ~4k):**
- **scp_head_node provider (~180 LOC)** — the *documented default*, and **no env can
  select it** (`longleaf` is globus, `my_mac` is local → `None`). Zero of 92 records
  use it. Zero tests drive it.
- **async Globus + `globus_task_status` reconciliation (~250 LOC)** — **zero
  `"outcome": "submitted"` records have ever existed.** And it's the broken half:
  `globus_task_status` returns `success: True` even when `status == "FAILED"`; the
  async path omits the entire PERMISSION_DENIED classifier it spends ~200 LOC
  building; `finalize_manifest_for_task` swallows `OSError` and still reports
  `"uploaded"` while the on-disk record says `submitted` — defeating the exact
  half-baked-transfer defense it exists for.
- `reference_data_targets[]` — documented in the YAML, **zero Python references**;
  loads silently, does nothing, and is excluded from the disjointness check.
- `job_manager: bash`, GPU submission — enum-validated, no live path.

**Bugs worth fixing:**
- **Test leakage into the live repo, active today**: `job_submissions/demo/…` (`host:
  fake.example.edu`) from `submit_workflow.py:280` writing to a CWD-relative path in
  an un-chdir'd test; **56 junk records** under
  `transfer_history/c4_nonexistent_project_zzz/` because `test_c4_crash_safety.py`
  sits outside L14 and `transfer._repo_root` is `__file__`-derived (chdir can't help).
  Same class as the previously-fixed draft leak. Needs a root `tests/conftest.py`.
- `check_permission` (`compute_access.py:834`) `rstrip("/")` with no `normpath` —
  `…/PLANT_PROJECT/../../../../etc/passwd` **ALLOWs**. Not reachable today (all 3
  callers pre-reject `..`), but the auth gate should self-defend. One `normpath`.
- `projects_access.yaml`'s own schema header documents a `fetch` token and
  `slurm.queue_default` that **hard-error** if used, and names 4 primitives that no
  longer exist. `agent_scratch_targett` (typo) loads clean and the grant silently
  vanishes.

## 7. What to do — ordered by (value / effort)

> **TIER 0 — COMPLETE.** All items below done + verified by driving the real surface
> (not just the suite). Suite: 1251 → **1264 passing**, exit 0, zero repo leakage.
> Live result: the reliability gate fires for the first time —
> `talos`/`hap.py` → `authors_recipe`, `samtools`/`MultiQC` → `conda`.
> Each fix was proven by reintroducing the original defect and watching the new test fail.
> See §10 for what the self-audit changed about this plan.

**Tier 0 — one-line fixes that resurrect shipped features (do first, today)**
1. `resolver.py:728` — drop `timeout=timeout`. Resurrects the entire reliability gate.
2. `resolver.py:737` — replace `except Exception: pass` with a recorded
   `probed["authors_gate_error"]`. *A silent gate is worse than no gate.*
3. `freeze_from_image.py:181` — set `record["license_gated"] = gated`. Resurrects I13.
   Better: make `freeze_record` emit `license_gated` and stop carrying two names.
4. `env_freeze.py:644` — add `"r_github"` to `_R_TIERS`.
5. `spec_writer.py:609` — `and` → `or`; make an absent sacct MaxRSS `None`, not `0.0`.
6. Fix the 3 gate tests to inject `get_text`/`get_json` (the seam already exists)
   instead of replacing the function reference.
7. Add a root `tests/conftest.py` isolating `transfer_history/` + `job_submissions/`;
   delete the 57 leaked test records from the live audit trail.

> **TIER 1 — COMPLETE.** Suite 1264 → **1283 passing**, exit 0. Live campaign: **4/4**
> collision names caught, **29/30** real bio tools confirmed, **zero** wrongly accused.
> See §11 — item 6 below was a misconception and item 7's "refuse" was the wrong verb.

**Tier 1 — close the identity hole (§3). The highest user-facing value in the repo.**
6. ~~Run the cross-namespace guard **always**, not only when `github_repo` is passed.~~
   **WRONG — see §11.** That guard compares registry hits to a *user-supplied* repo; with
   no repo there is nothing to compare against.
7. Add a plausibility check on the chosen registry entry (summary/homepage vs the
   requested tool). ~~Ambiguous → refuse, don't guess.~~ → **disclose**, don't refuse.
8. Stress campaign: 30 real bioinformatics tool names through `resolve_tool`, scored
   by a human on "is this the tool I meant?". This is the campaign that matters most.

> **TIER 2 — COMPLETE.** Suite 1283 → **1297 passing**, exit 0. Investigated with a
> 7-agent adversarial workflow BEFORE implementing — which overturned item 9 outright.
> See §12.

**Tier 2 — make the contract mean what it says**
9. ~~Gate evidence depth for **requested** tools~~ — **DON'T. Measured: it refuses the
   flagship-CORRECT artifact and the known-BROKEN one identically.** See §12. The
   classifier was fixed (it was wrong as *disclosure*); nothing gates on it.
10. Validate the adopt path in-image. ~~(reuse `freeze_from_image`)~~ → **extract**, don't
    reuse (record-shape regressions); and fix the **producer first**. `check_adopt` deleted.
11. Move I4 into the frozen image and delete the `and draft.get("conda_env")` escape.
    ~~Either I4 gates the seal or it doesn't.~~ → the deliverable is **three-state
    `usage_verified`**; `not_attempted` must seal.

**Tier 3 — build goal 2 (the actual product gap)**
12. `{name}.HOWTO.md`: agent-authored, multi-step, vanilla commands first, explicitly
    labelled "authored, not verified", rendered beside the verified evidence.
    Model it on the hand-written `talos_authors.USER_GUIDE.md` — that file is the spec.
13. Make `usage` hold a **sequence** of steps, not one `command_template: str`.
14. Seal one genuinely multi-step pipeline end-to-end. Until that exists, "pipeline
    generation" is unproven.

**Tier 4 — subtract**
15. Delete `env_vendor.py`; expose or delete `build_env_from_tools`; unify the 4
    image-digest helpers; unify the 2 `mode` vocabularies; fix or drop the spack tier.
16. Cut the MCP surface toward the ~25 documented primitives.
17. Sync `resolve_tool`'s docstring with reality (it's the routing prompt).
18. HPC: delete the scp provider (unreachable) and either fix or delete async Globus
    (never run, and broken); drop `reference_data_targets`; fix the YAML schema header.
    Keep the four zones — they're earned.

---

## 9. On the next stress campaign

The campaign that matters is **not** more firewall probes — `docs/stress_findings.md`
shows those are already well-certified, and none of the 8 defects above would have
been caught by another one. Every defect above was found by **driving the real
surface and reading the real artifacts**, because the failures live precisely in the
gap between what the code says and what it does. So:

1. **Identity campaign (highest value).** 30 real bioinformatics tool names →
   `resolve_tool` → a human scores "is this the tool I meant?". This measures the
   §3 hole, which no invariant can see.
2. **The multi-step campaign.** Seal one real 3+ step pipeline. Everything about
   goal 2 is unproven until this exists.
3. **The anti-mock campaign.** For each of the ~10 load-bearing internal seams, one
   test that calls the real callee with injected I/O. The 137 lambda doubles are
   where the next §2-class defect is already hiding.
4. **Artifact review, not test review.** After any campaign, read the rendered
   ENV.html/RUN.html/recipe.md as a user. Three of the falsehoods in §4 and §6 are
   visible in 30 seconds of reading output and invisible to any assertion.

---

## 10. Tier 0 post-mortem — what the self-audit changed

Three things worth recording, because two of them correct THIS document.

**1. The audit's own I7 fix was wrong.** §2/#8 said "fix `and`→`or`". I made that change,
then drove the real host monitor and found it false-refuses real, successful steps:

```
true              rc=0 wall=0.01 peak_rss_mb=0.0 samples=1
echo hi           rc=0 wall=0.01 peak_rss_mb=0.0 samples=1
python3 -c pass   rc=0 wall=0.05 peak_rss_mb=0.0 samples=1
sleep 0.5         rc=0 wall=0.52 peak_rss_mb=1.0 samples=2
```
`env_manager._run_monitored` polls the process tree every 0.3s, so any step faster than
that legitimately reports zero RSS. That is a **sampling limit, not fabrication** — and a
value-level check cannot tell the two apart. `or` would have refused every fast host step
at seal.

The defect was real; the fix belonged in the **producer**, which is the only layer that
knows which case it's in. `cluster_jobs._parse_max_rss_mb` now returns `None` for "sacct
accounted nothing" (was: `0.0`, indistinguishable from a measurement) and
`cluster_job_resources` attaches an explicit `sacct_error`, which I7 already refuses.
`and` is retained, with a comment explaining why tightening it is a trap.

**Generalised rule, now applied in three places: absence of data must never render as
data.** Gate fails → `authors_gate_error`. sacct has no MaxRSS → `sacct_error`. SBOM probe
fails → `sbom_error`. Each replaced a silent degradation to a plausible-looking value
(no author tiers / `0.0` MB / `[]` packages) that downstream rendered as an observation.

**2. One more instance of the pattern, found while sweeping.** `freeze_from_image.py:147`
wrapped the SBOM capture in `except Exception: pass`. A probe failure left
`resolved_packages == []`, which the ENV report renders as "0 along for the ride" and the
attestation carries as an empty package list — the *headline* Layer-1 deliverable silently
vanishing. Now recorded as `sbom_error` (disclosure, not a refusal: some images genuinely
have no conda prefix). The other ~10 bare swallows in `agent/` are benign best-effort
enrichment (JSON coercion, mtime, fastq length scans) and were left alone.

**3. Fixing the gate armed the latent `route()` gap, as predicted (§6).** `route()` now
defers on `author_image`/`authors_recipe`. Blast radius is **zero for the agent**:
`route()` has exactly one caller, `build_env_from_tools`, which no MCP tool exposes, and
the agent's real path (`resolve_tool` → `install_call` → `build_env_from_authors_recipe`)
works. Its misleading "pip needs engine pypi support" message is fixed to name the real
executor; the structural fix is entangled with the Tier-4 "expose or delete
`build_env_from_tools`" decision and stays there.

**Also corrected in Tier 0 (found by self-audit, not in the original plan):**
- `resolve_tool`'s docstring — the agent's actual routing prompt — still described the
  pre-gate ranking. Turning a feature on while its description omits it is only half a
  fix. Now documents the gate, the tiers, and `authors_gate_error`.
- `submit_workflow`'s manifest root was CWD-relative while `transfer`'s was repo-anchored;
  that inconsistency is what let the `fake.example.edu` record escape. Both now use one
  anchor, so test isolation patches one function.

**On the anti-mock sweep (§9/#3):** an automated signature-drift scan over the 137 lambda
doubles produced 29 hits that are ~all false positives — doubles renaming *positional*
params (`lambda n, t=12` vs `(name, timeout=12)`) are harmless. The killer pattern is
narrower: **production passes a keyword the callee lacks**. That needs call-site binding,
not signature comparison, so the campaign stands as real work rather than a script.

## 11. Tier 1 post-mortem — identity

**What shipped.** `resolve_tool` now returns an `identity` block answering the one
question nothing else in the system asks. Anchors, strongest first: `github_repo` (the
caller named the project) · `bioconda-channel` / `bioconductor` (**membership of a
bio-only channel IS identity, by construction** — exact, free, and it covers 29 of the 30
tools in the campaign with no heuristic at all) · `repo` (repo-backed tiers) ·
`domain-terms` (its own self-description reads like bioinformatics) · `none`.

The load-bearing change is smaller than it sounds: **the probes now capture the package's
own `summary`**. They never did. CRAN's `cellranger` has always announced itself as
"Translate Spreadsheet Cell Ranges to Rows and Columns" — the resolver just never looked,
so no layer, human or machine, had any basis on which to notice.

**Two corrections to this document's own plan:**

*Item 6 was a misconception.* "Run the cross-namespace guard always" is not well-defined:
that guard checks a registry hit against a **user-supplied** repo, and `_github_owner_repo`
extracts the repo *from* pip metadata — so checking pip against its own repo is circular
and always passes. Nothing to compare against without an external anchor. cellranger was
never a collision problem (it exists in exactly one ecosystem); it was an evidence problem.

*Item 7's "refuse" was the wrong verb.* Identity is a question about **intent**, and no
invariant can see intent — this is the Tier-0 lesson again (don't gate on something the
checker can't see; surface it to the layer that can). So identity **discloses**: when
unconfirmed, `install_call` is prefixed with the reason, because install_call is the
string an agent copies and a warning parked in a sibling key is a warning that gets
skipped. `numpy` still resolves — it is simply, and correctly, told it isn't a
bioinformatics tool.

**Calibration matters as much as detection**, and the campaign measured both:

| | |
|---|---|
| collision names caught | **4/4** — incl. `gab` → *"Python client library for the Gab.com API"* (this codebase's own GAB bug) and `ape` → *"A Productive Environment - make/rake/ant/fab-like system"* |
| real bio tools confirmed | **29/30** |
| **wrongly accused** | **0** |

The first campaign run flagged `vep` and that exposed a real design flaw: I was conflating
*contrary* evidence with *no* evidence. cellranger's description says "spreadsheets" — that
is a reason to suspect the pick. The spack tier publishes nothing — that is a reason to
suspect our probe. Reporting both as "may not be the tool you mean" is exactly the noise
that trains a reader to skip warnings, and a skipped warning is worth nothing. Split into
`reason='no_domain_signal'` (evidence against) vs `reason='no_description'` (no evidence),
which drops the wrongly-accused count to zero.

**Known residual:** the spack tier carries no metadata, so every spack pick is
`no_description`. That is honest, but it is also more evidence for the Tier-4 "fix or drop
spack" call — a tier that can never anchor identity and was never real-build-tested.

## 12. Tier 2 post-mortem — the contract

Investigated by a 7-agent adversarial workflow (investigate → refute → synthesize) before
a line was written. It was worth it: **it overturned the biggest item and caught two
false-refusals the plan would have shipped.**

**#9 depth gate — NOT DONE, and this document was wrong to ask for it.** The decisive
argument is the corpus, not the design. On the three real records that exist, a depth gate
refuses the flagship-CORRECT artifact and the known-BROKEN one *identically*:

| record | evidence | shallow? |
|---|---|---|
| `talos_authors` — the authors'-image build that **really carries** the bcftools fork | `[help, version, version]` | all |
| `talos_v11` — the **broken** reconstruction that dropped it | `[import]` | all |

Zero discriminating power on the exact war story it was designed for. And the war story
doesn't support it on the merits: Talos shipped broken because bcftools/htslib/echtvar had
**no evidence rows at all** — not because talos's row was shallow. A *functional* talos
check that never shells out to bcftools would have passed too. It is also one-character
bypassable (`| cat`) and unsatisfiable on conda by construction.

The classifier was nevertheless **wrong as disclosure**, which is reason enough to fix:
`command -v samtools` read `version` (matching the `-v` of `command -v`);
`_conda_presence_check` — freeze's OWN probe, the evidence on nearly every real record —
read `functional` because `/opt/conda/.../conda-meta/*.json` matched "reads a file". The
weakest evidence in the system reported as the strongest. Now: plumbing stripped first,
version/help decided before the path rule, a real `presence` depth, and `unknown` instead
of guessing.

**#10 adopt validation — DONE, and the producer had to be fixed first.** Measured against
real biocontainers, the gate would have false-refused **4 of 7 healthy packages** (gatk4
rc=1, htslib rc=127, perl-bio-db-hts rc=1, perl-bioperl rc=1) because BioContainers install
their conda prefix at `/usr/local`, not `/opt/conda`, and a `perl-*` module library ships
no `bin/` entry at all. `perl-bio-db-hts` → `Bio::DB::HTS` is not mechanically derivable
from the package name, so the probe reads the module out of the package's **own** conda-meta
`.pm` records instead of guessing. All 8 now pass. *Then* the gate went in.

Real result — `freeze(samtools=1.21)`, live: `verifications: []` → **1, rc=0, passed**;
`validation_locus: "adopted"` (i.e. nowhere) → `emulated` (a real locus); digest unchanged
(`sha256:d1585753…`, so the two sealed workflows and the on-cluster `.sif` still agree).
`check_adopt` is deleted — one contract for both modes. Its I13 clause was dead anyway
(`can_adopt` requires `not gated`). `ADOPTED_BY_DIGEST` survives in the attestation and ENV
report, because it is a *provenance* claim ("we did not build these bytes") and that was
never the part that was wrong; VALIDATED_IN_IMAGE is now asserted off the **evidence**,
never off the mode.

**#11 I4 — DONE, but the deliverable was not the hard gate this document asked for.**
Since seal *refuses* a failed self-test, every `usage_verified: False` that reached disk
provably meant "never attempted" while the dashboard rendered it as a verdict. Three-state
`{verified | failed | not_attempted + reason}` is the fix; a container-backed runner
(satisfying the *existing* `run_in_env` seam, so `spec_writer` learns nothing about docker)
is what makes it verified more often. Driven against all 4 real sealed specs:
`cluster_refdata_validation` False → **verified in image**; `samtools_cluster_rung3` True
(host) → **verified in image**; the two Talos workflows → `not_attempted` + a real reason.
All four still seal.

**Two false-refusals I introduced and caught by driving real code — the Tier-0 rule, twice
more:**
1. My precondition inspected `usage.trials` while `self_test_usage` *infers* substitutions
   from `pipeline_steps[*].inputs` when trials are empty. It therefore guarded nothing, and
   `talos_validate_moi` (trials `[]`, inference fills 2 of 5 slots) self-tested as **failed**
   → **unsealable**. A precondition must inspect the values the runner will actually use.
   An incomplete inferred trial is now `not_attempted`: the how-to was fine; our trial wasn't.
2. Seal read `status == "failed"` and sealed on anything else — so a result carrying the
   **old two-state shape** (`{ok: False}`, no status) silently disarmed the H2 gate. It was
   a live test double. Now fails closed. *Mock-shape drift again, exactly as in Tier 0.*

**Also fixed (surface):** the RUN dashboard asserted *"self-tested against every declared
input shape (I4)"* **unconditionally** — on all 4 pages, including one with no usage block,
directly above a "not self-tested" pill — and its footer claimed *"no field on this page was
authored by the agent"* while rendering `description`, `usage.description`, and
`command_template`, all in patch_pipeline's agent-authored allowlist. It now separates
machine-observed evidence from authored prose.

**Deferred, with reason:** the conda `functional_check` seam. Evidence is invisible to
`request_key` and `content_digest`, and `freeze()` short-circuits on a cache hit *before*
any build — so an agent supplying deeper evidence for an already-frozen env would get a
cache hit and the old record back, and the new evidence would never run. "Gate present in
code, absent in effect," freshly minted. Since nothing gates on depth, nothing forces deeper
evidence yet: build neither until a real consumer exists.

**Known residual:** ~~the `samtools=1.21` record on disk still has `verifications: []`~~ —
**CLOSED in the re-audit, and it was never a "residual"**; it was the visible end of a
structural hole. See §13.

---

## 13. THE RE-AUDIT (2026-07-16, second pass) — the disease has ONE cause

Five parallel adversarial audits re-drove Tiers 0/1/2 and the diagnostics. **All three
tiers landed real fixes. All three left the fix unreachable from where it mattered**, and
the reason is the same every time — which is the finding this whole document was missing.

### The cause: one truth, defined in N places, drifting in N−1

§2 named the *symptom* ("gates present in code, absent in effect") and treated the 8
instances as 8 bugs. They are one bug. Every single instance is a concept with **more than
one definition**, where the copy that runs is not the copy that is correct:

| the one truth | where it's defined | the copy that drifted |
|---|---|---|
| "is this artifact trustworthy?" | `env_honesty.check_build` | 3 PRODUCERS call it; **4 SERVERS don't** — freeze's cache hit, `run_step_in_container`, `stage_apptainer_image`, `seal_workflow` |
| "is this evidence shallow?" | `_SHALLOW_DEPTHS` | `env_report_html.py:190` + `freeze_from_image.py:246` hardcoded a 3-tuple omitting `presence` → the WEAKEST evidence rendered "runs the tool" |
| "was usage self-tested?" | three-state `usage_verification.status` | `run_dashboard_html.py:395` rendered the raw bool → header said "False", body said "not attempted", same page |
| "did sacct measure RSS?" | `_parse_max_rss_mb` | Tier 0 compared string literals `"" | "0"`; real SLURM emits **`0K`** → fabricated zero sealed through I7 anyway |
| "which terminal is on this line?" | the outcome `code` | coverage keyed by `file:line`; lines moved → **2 named FIREWALLS credited as certified from measurements taken for other terminals** |
| the seaworthy meter | `seaworthy_scope.summarize` | hand-typed into `seaworthiness.md` (34/124) and a memory (77/125); generator said 87/142 and was right all along |
| the EnvCache record | `freeze.freeze_record` | `env_build.to_cache_record` — a 2nd builder, 3rd `mode` vocabulary, and it OMITTED `verifications`, i.e. it minted the samtools defect by construction |

This reframes Tier 4 entirely. **Subtraction is not cleanup here — it is the fix.** Every
duplicate is a future instance of §2 waiting to happen.

### What the re-audit changed (fixed, driven, proven by reintroduction)

**1. The cache-hit hole — the one that could hand a user a bad artifact today.**
`lookup_anchored` re-anchored only BUILT (image presence) while its own docstring promised
"a hit is a claim until re-checked". Proven live: `samtools=1.21` **fails `check_build`
today** and `freeze()` returned it as `proven` on every hit. So Tier 2's adopt-validation
gate was live in code and absent in effect **for every env that already existed** — the
disease reproducing inside its own cure. §12 even named this mechanism when deferring
`functional_check` ("freeze short-circuits on a cache hit before any build… gate present in
code, absent in effect, freshly minted") and did not apply it to the fix it had just made.

Fixed by re-anchoring the FULL contract in `lookup_anchored`, + `lookup_verified` for the
three serving paths (which now name the failing clause instead of reporting "not in cache").
The general property is what matters: **a strengthened gate now reaches artifacts frozen
before it existed.** Without it, every future contract change is retroactively opt-out.

Live result — the record self-healed with **no manual backfill and no digest churn**:
`cache_hit: False` → re-adopt → `verifications: []` → **1, rc=0, passed**;
`validation_locus: adopted` → `emulated`; digest **unchanged** (`sha256:d1585753…`, so the
two sealed workflows and the on-cluster `.sif` still agree). And because a MISS re-runs the
deliverable renderers, it also wrote the `recipe.md` + `recipe.yaml` that env had never had
— closing CLAUDE.md's "recipe ALWAYS, for EVERY install path" claim, which was false on 2 of
3 envs. One fix at the chokepoint, four documented-but-false claims closed.

**2. I7 still fabricated a zero** (`0K`/`0M`/`0.00M` — the shape a cluster with no cgroup
accounting actually emits). The Tier-0 fix compared spellings; the test parametrized
`["0", "", "   ", …]` and pinned the incomplete contract. Now numeric, after the parse.
*The audit's original finding #8 survived its own fix for one full tier.*

**3. Two render sites re-derived a guarantee** instead of asking the classifier (see table).

**4. `to_cache_record` + `build_or_cached` DELETED** (zero production callers). This is the
model for Tier 4: the "2 divergent `mode` vocabularies — latent landmine" wasn't a
unification job, it was **dead code**, and deleting the only producer removed the divergence
outright.

**5. `capability_probe` can now FAIL.** This is the most damning finding in the re-audit:
`docs/capability_map.json`, committed **2026-07-03**, contains
`tool: cellranger, chosen: cran, note: "COLLISION: CRAN cellranger != 10x cellranger"`.
The probe drove the real resolver, correctly classified the wrong-package route, wrote it
down — and `return 0`. **Thirteen days later §3 "discovered" that same defect and called it
the most user-facing hole in the system.** Detection was never the bottleneck. `note` was
prose, so nothing could fail on it. `expect` is now machine-checked; reintroducing the
identity bug makes the probe exit 1. Its first act after gaining assertions was to catch a
stale claim of its own (`ape(r)` routes to conda's `r-ape`, not CRAN — the router improved,
the note didn't).

### The rule this yields (add to the three in §10/§11)

**4. One truth, one definition, read at every use.** Never re-derive; never hand-copy;
never let a second producer of the same concept exist. When a guarantee is duplicated, the
copy that drifts is always the one being read — because the correct one is the one people
maintain. Corollaries, all observed above: a gate must be asserted where an artifact is
*served*, not only where it is *produced*; a number in prose is a number that rots; a
diagnostic that reports without asserting will be ignored for as long as it takes someone
to rediscover the bug it already found.

### Why 1297 tests still missed all of this

Same cause, one level down. **20 test fixtures registered EnvCache records that no producer
can emit** (`{"image": …, "image_digest": …}`, no `verifications`) — all three producers
gate on `check_build`. The suite agreed with itself that an evidence-free record was normal,
so nothing objected when the cache served one as `proven`. A fixture that cannot occur in
production proves nothing about production. Contract-clean builders now live in
`tests/env_records.py`; `FakeEnvCache` was a hand-rolled double that implemented only
`lookup()` and is now a real `EnvCache` with its persistence swapped — **override the I/O
seam, inherit the logic**, which is §2's lesson applied to the doubles themselves.

The retired `test_env_build_cache_hit_returns_proven` deserves an epitaph: its docstring
read *"Cache-hit honesty is load-bearing — a wrong hit ships a wrong env — so the green must
reflect a real anchored lookup"*, and it then passed the code a `_FakeCache` returning a
hard-coded dict. It named the exact risk and mocked away the only thing that could detect it.

### Still open after the re-audit (see §14)

`mark_step_validated` (an agent-asserted override of all three I3 clauses — the one thing
CLAUDE.md swears never happens); `verify_env_recipe` passing only by accident of a
containerd-store Mac; the identity disclosure never reaching disk; the `author_image` tier
100% dead behind a silent 401.

## 14. The REVISED plan — reordered by what the re-audit learned

§7's tiers were ordered by (value/effort) against a symptom list. With the cause known
(§13), the order changes: **the deletions are no longer last.** Duplicate definitions are
where the next §2 defect is already sitting, so subtracting them is hardening, not tidying.

> **TIER 5 — COMPLETE (the re-audit's own fixes).** Cache-hit contract re-anchor +
> `lookup_verified` on the 3 serving paths · `0K` numeric guard · 2 render sites read the
> classifier · `to_cache_record`/`build_or_cached` deleted · `capability_probe` asserts ·
> contract-clean test fixtures · the drifted numbers struck from `seaworthiness.md`.

> **TIER 6 — COMPLETE.** All four done + proven by reintroduction. The headline: the
> `author_image` tier **fired for the first time in the project's history**
> (`uv` → `author_image`; `talos` → `authors_recipe`; `samtools` → `conda`).
> See §15 for what the fixes changed about this plan — two of the four diagnoses below
> were wrong in a way that mattered.

**TIER 6 — the switches that hand out a green. Do first: these are user-facing lies.**
1. **`mark_step_validated`** (`workflow_tools.py:942` → `spec_writer.py:464/483/504`) — a
   blanket agent-asserted override of ALL THREE I3 clauses, including the C1 amendment that
   exists *specifically* to stop sealing over outputs that demonstrably FAILED validation.
   A step whose every validation record is `passed: False` can be marked `passed` and
   sealed. Its own test docstring says "the agent asserts it verified the output by other
   means" — which is verbatim what CLAUDE.md:16 promises never happens ("nothing is taken
   on faith from the agent"). **Undocumented in CLAUDE.md**, so no reader of the contract
   can know the hole exists. Zero uses in the corpus ⇒ narrowing costs nothing. Narrow to
   the no-outputs case; never let it suppress `passed: False`.
2. **`verify_env_recipe` passes only by accident of this developer's laptop.** Its adopt
   branch compares `docker inspect {{index .Id}}` against a *registry manifest* digest.
   These are equal ONLY under the containerd image store (verified: this Mac runs
   `io.containerd.snapshotter.v1`, and `.Id == RepoDigests` for the samtools image). On a
   classic overlay2 daemon `.Id` is the config blob → **"recipe not reproduced" for every
   adopt recipe, for every normal user.** A verification tool that lies on the majority
   platform. Fix `.Id`→`RepoDigests` BEFORE unifying the 4 digest helpers, or the dedupe
   cements the bug as the shared answer. (`freeze()`'s own adopt is correct —
   `biocontainers.py:198`; `freeze_from_image.py:119` records `.Id` and is latent.)
3. **The `author_image` tier is 100% dead and dies silently** —
   `api.github.com/orgs/{owner}/packages` → **401**, swallowed in `_default_get_text`
   *below* the `authors_gate_error` recorder, so the gate reports "fired successfully,
   authors ship no image" when the truth is "we were denied". Tier 0 resurrected half the
   reliability gate; CLAUDE.md advertises both tiers as live. Talos *publishes* an image and
   we still route to `authors_recipe`. Either plumb a token or **drop the tier and say so**.
4. **`authors_gate_error` is written by nobody's reader** — no consumer, and no test fails
   when it reverts to `except: pass` (verified: suite stays green). A gate error must poison
   `install_call` like the identity and collision paths already do (`resolver.py:1022,1048`),
   or it is decoration.

**TIER 7 — subtract (was Tier 4). This IS the hardening; ~1,900 LOC.**
- **Diagnostics (~1,300 LOC):** DELETE `scenario_decision_tree.{json,md,html}` +
  `render_decision_tree.py` (a hand-authored literal masquerading as generated output; zero
  mentions of `author_image`/`authors_recipe`/`freeze_from_image`); DELETE
  `system_dimensions.md` (hand-maintained checkboxes, superseded by the dashboard) and
  `stress_test_campaign.md` (self-declared superseded). KEEP+FIX `capability_probe` (done),
  `measure_terminal_coverage` (**key by `code`, not `file:line`** — R1: the producer knows),
  `render_outcomes_dashboard` (stamp generated-at + HEAD SHA; refuse to render when SHA ≠
  HEAD — R3). KEEP `agent_status`, `extract_outcomes`, `seaworthy_scope`, the source-tag
  ratchet: they were never the liars.
- **Dead code (~570 LOC), function-level (which is why a module sweep missed it):** the
  `env_manager` retired-finalize inventory (170) · `resolver.route` + `build_env_from_tools`
  (189 — **DELETE, don't expose**: it never registers in the EnvCache and writes no
  deliverables, so it returns a `request_key` nothing can consume, and post-Tier-0 it
  *defers* on exactly the author tiers the gate exists for) · `agent/models/__init__.py` +
  `KNOWN_PIPELINES` (43) · `content_digest_parts`/`content_digest_from_spec` (44) ·
  `lookup_tag_by_digest` (34) · `OutputValidator.infer_type` (23 — a dead twin of
  `run_tools._infer_validator_type` with a *divergent* vocabulary) · `container_build.write_file`
  (21) · `env_vendor.py` (77).
- **Unreachable schema:** `install_method.type` `docker_pull`/`manual` have **zero
  producers** — and `freeze.py:298` branches on `docker_pull` to decide adopt-vs-build.
  `PipelineSpec.docker`/`DockerBuild` have no producer but **3 live readers** that are
  therefore permanently dead (`list_installed_pipelines` always reports `docker_image: None`).
  `core_data.py:6` documents the two values with no producers and omits the four with them.
- **Spack: DROP.** All 5 claims verified. Worse than the audit thought: freeze *can* build
  it, so the mis-probe is a live false positive (`resolve_tool("vep") → spack`, live), and
  it can never anchor identity (no metadata).
- **HPC — the audit was wrong twice; do NOT delete:** `scp_head_node` is the *reachable
  default* for the next ssh env a user adds (`compute_access.py:710`) — it's a **test** gap,
  not dead code. GPU submission is fully wired and config-gated. **DELETE** async Globus +
  `globus_task_status` (~224 LOC, zero `submitted` records ever, and broken three ways —
  largest real transfer was 1.0 GB *sync* in 1m25s vs a ~600s watchdog; the need never
  materialized) and `reference_data_targets[]` (zero code). **KEEP the four zones** — earned.
- **MCP surface: 65 real (not 69) → ~59.** Promote `synth_fetch`/`synth_build` (the resolver
  *emits them as `install_call`* while CLAUDE.md never documents them), `create_conda_env`,
  `agent_status`. Delete `install_spack_package`, `install_pipeline_brief` (a hand-maintained
  duplicate of CLAUDE.md that has drifted — it teaches agents the **pre-gate** protocol),
  `globus_task_status`.
- **Three one-liners:** `check_permission` `normpath` (traversal ALLOWs today — unreachable,
  but an auth gate must self-defend) · closed-key validation for compute envs
  (`agent_scratch_targett` loads clean and the grant silently vanishes) · fix the
  `projects_access.yaml` schema header (documents a `fetch` token + `slurm.queue_default`
  that hard-error, names 4 primitives that no longer exist).

**TIER 8 — identity, finished (was Tier 1's unfinished half).**
- The disclosure **never reaches disk**: `grep identity` in `env_honesty`/`attestation`/
  `spec_writer` still returns nothing. It lives only in a chat transcript. An artifact
  cannot be marked identity-unconfirmed, so §3's end state is unchanged for anyone reading
  a report later. Persist the resolved package's **own self-description** into the record
  and render it in `ENV.html` — "what did I actually install, in the tool's own words" is
  the question goal 3 exists to answer.
- **The bioconda axiom is false and short-circuits contrary evidence.** `perl-json` is on
  bioconda ("JSON encoder/decoder"). Worse, `:594` returns *before* `domain_signal` at
  `:603`, so a bioconda entry describing itself as a spreadsheet parser is **confirmed**
  with the contradiction never computed. And which anchor you get is a **version race**
  (`probe_conda` keeps the higher version, so `biopython`/`ete3` are on bioconda yet fall to
  the text heuristic). Membership answers "is this bio?", never "is this the one you meant".
- **`_DOMAIN_TERMS` fails both ways** and is essentially unmeasured (bioconda covered 29/30,
  so the campaign never exercised it): false-accuses `anndata` ("Annotated data.") and
  `pyranges` ("GenomicRanges for Python." — `genomic` is listed but the word-boundary regex
  won't match inside `GenomicRanges`); plural-blind ×12; false-confirms on `pdb`, `sam`,
  `ontology`, `taxonomy`, `variant`, `chromosome`, `transcript`. Its own comment claims
  precision-bias; the list is recall-biased general English. **Consider deleting the scorer
  and showing the summary instead** — the signal that caught cellranger and talos was never
  "does this look bio?", it was the package's own words contradicting the request. Show the
  evidence; let the reader judge. No scorer, no false accusations, no false confirmations.

**TIER 9 — goal 2 (the product gap). Deliberately LAST, and the user is right about that.**
The guides + the installation report's look/contents are **cosmetic and need the user in the
loop** to get right, and they depend on everything else being true first — the hooks were
built deliberately so they can be designed later. This document's §4 framed that as
misplaced priorities ("the fun half got built"). That was wrong, and the re-audit is the
evidence: the RUN dashboard asserted *"self-tested against every declared input shape"* on a
page with no usage block, and footered *"no field was authored by the agent"* while
rendering three authored fields. **Designing that page before the record under it was honest
would have been designing a liar's face, carefully.** The renderers are pure functions of
the record; if the record lies, better typography only makes the lie more persuasive.

> **The ONE piece of goal 2 that was NOT cosmetic — DONE (2026-07-16).**
> `command_template: str` → `Union[str, list[str]]`. `pipeline_steps` was always a list and
> I8 lineage already held across a chain, but the **how-to contract** — the thing a user
> reads and runs, and the thing the guides will render — could only ever say ONE command,
> so a Phase A→B→C pipeline was **inexpressible in the data**. No amount of later guide
> design fixes data that cannot say what you mean, so it belonged in the foundation.
> A list runs IN ORDER sharing one working dir (step 2 consumes step 1's output); the first
> non-zero rc fails the trial and STOPS, so a later command can never produce the declared
> outputs over a broken earlier one. Proven by driving the real `self_test_usage`: a genuine
> 3-phase how-to → `verified`; a broken middle step → `command 2 of 3 execution failed
> (rc=127)`, with command 3 never run. A bare `str` still means what it always did (all 4
> sealed specs on disk are `str` and still validate).
> Per rule 4, the two shapes get **exactly one reader** — `core_data.usage_commands()` —
> and nothing else may branch on the type. Two spellings of one field is how a renderer and
> a self-test come to disagree about what the workflow is.

Still open and genuinely the user's call: `{name}.HOWTO.md` (agent-authored, vanilla-
commands-first, labelled "authored, not verified", beside the verified evidence), the
installation report's contents/look, and sealing one real multi-step pipeline end-to-end.

---

## 15. Tier 6 post-mortem — two of the four diagnoses were wrong

**#2 `verify_env_recipe` — CONFIRMED and worse than described.** Verified on the real
recipe: `content_digest: sha256:d1585753…` IS the biocontainer's published *manifest*
digest (it is literally inside `adopt_image: repo@sha256:d1585753…`), and the branch
compared it to `docker inspect {{index .Id}}`. This Mac runs the containerd snapshotter
(`docker info` → `io.containerd.snapshotter.v1`), where `.Id == RepoDigests`; on classic
overlay2 `.Id` is the config blob. Simulated overlay2 → **"NOT REPRODUCED" for every adopt
recipe**. So the tool whose entire job is telling a user whether their env reproduces
passed by accident of one developer's docker config.

*The audit's "unify the 4 image-digest helpers" framing was wrong.* They are not four
copies of one concept — they answer **two different questions**, and `.Id` is the *correct*
answer to one of them (a locally BUILT image has no manifest digest until pushed). One
name, two meanings, wrong one in the load path — the §13 disease in its other form.
Named apart rather than unified: `image_digest` ("what are these bytes, locally?") vs
`registry_manifest_digest` ("what would anyone else pull?"). The adopt branch had **zero
tests**, which is why it survived; now parametrized over BOTH stores, because the bug is
invisible on exactly the one we run.

**#3 `author_image` — the diagnosis was right, the prescription ("plumb a token") was
wrong.** Measured live: `api.github.com/orgs/{owner}/packages` → **401 for nf-core,
bioconda, astral-sh** — every org, public ones included. That REST API *always* requires
auth, so the tier could not have worked even WITH a token for an unauthenticated user, and
`/orgs/` excluded user-owned repos besides.

It was the wrong **question**. We never needed "is a package listed under this org?" — we
needed "can we pull this image?", which the registry answers anonymously via its own token
flow: `ghcr.io/astral-sh/uv` → **HTTP 200, no credentials**. And a private image is one we
could never adopt, so a denial is a *true negative*, not a gap. Rule 1 again — don't ask an
oracle that can't see; ask the one that knows.

Also corrected here: the Tier-0 comment claimed *"the gate is either reported as fired or
reported as errored; there is no third state."* False — the 401 died in `_default_get_text`
*below* the recorder, which is the third state. And my own first cut of the fix inverted
R2 the other way: it reported ghcr's DENIED (a true negative) as a probe error, which would
have stamped "[NB: probe FAILED]" on every cleanly-packaged tool — the exact noise that
trains a reader to skip warnings, i.e. the §11 calibration lesson, re-learned by driving it.

**#4 `authors_gate_error` — CONFIRMED: the fix for the silent gate was itself silent.**
No consumer, and reverting it to `except: pass` left the whole suite green. Now the gate
error (and a failed image probe) poison `install_call`, the same treatment identity and
collisions already got, plus the guard test that was missing.

**#1 `mark_step_validated`** — done in Tier 5.

**The pattern across all four:** every one was a *disclosure* that existed and reached
nobody. The value isn't in computing the warning; it's in putting it where the reader
cannot avoid it. `install_call` is that place, because it is the string an agent copies.

## 8. The one-sentence version

The reproducible-env engine is strong and close to done; what's missing is that
**its guarantees are checks that don't run — not because anyone skipped them, but because
each one is defined in two places and the copy that runs is the stale one** — its trust
contract never asks whether the tool is the right tool, and the user-guide half of the
product exists only as a file a human wrote by hand and git ignores.
